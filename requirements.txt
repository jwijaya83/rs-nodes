openai-whisper
transformers>=4.46.0,<5
peft>=0.14.0
accelerate>=1.2.1
optimum-quanto>=0.2.6
bitsandbytes>=0.46.0
sentencepiece>=0.2.0
pydantic>=2.10.4
pyyaml
pillow-heif
imageio>=2.37.0
imageio-ffmpeg>=0.6.0
# RSEXRSequenceSave uses the official ASWF Python bindings instead of
# cv2 because opencv-python's prebuilt wheels ship with the EXR codec
# disabled (the OPENCV_IO_ENABLE_OPENEXR env var has to fire before
# cv2 imports, and even then some builds simply lack the codec).
OpenEXR
Imath
safetensors>=0.5.0
scenedetect>=0.6.5.2
av>=14.2.1
psutil
einops
# insightface 0.7.3's prebuilt wheel was compiled against numpy 1.x and
# crashes with "numpy.dtype size changed" on numpy 2.x. Pin numpy <2 until
# insightface ships a 2.x-compatible wheel.
numpy<2
insightface
onnxruntime-gpu
# Audio: vocal isolation (Demucs) + speaker embedding (speechbrain ECAPA-TDNN)
demucs
speechbrain>=1.0
# ROSE optimizer (used by RSLTXVTrainLora when optimizer="rose"). PyPI package
# is `rose-opt`, the import is `rose_opt`. Replaces the older
# git+https://github.com/MatthewK78/Rose path which no longer works.
rose-opt
# Attention: SageAttention for ~1.5-2x speedup on supported GPUs (auto mode in
# RSLTXVGenerate / RSLTXVICLoRAGuider falls back to default SDPA without it).
# Triton is a hard dependency of sageattention -- official `triton` has no
# Windows wheels, so use the community `triton-windows` fork there.
sageattention
triton-windows; sys_platform == 'win32'
triton; sys_platform != 'win32'
